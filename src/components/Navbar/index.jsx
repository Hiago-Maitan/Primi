import { FaShoppingCart } from "react-icons/fa";

export default function Navbar() {
  return ( 
    <header>
        <nav>
          <h2>Logo</h2>
          <FaShoppingCart />
        </nav>
      </header>
   );
}